# xterm.js

QuickFile-Go r48 includes a local xterm.js-compatible static build for the web terminal.

The bundled files are installed under:

`/www/luci-static/resources/quickfile-go/xterm/`

They are loaded locally and do not require CDN/network access.
