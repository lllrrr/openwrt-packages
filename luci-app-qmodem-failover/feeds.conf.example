# feeds.conf.example
# 将本插件加入 Lean LEDE 的 feeds，可以编译进固件
#
# ─── 使用方法 ───────────────────────────────────────────────
#
# 1. 进入 Lean LEDE 源码目录
#    cd /path/to/lede
#
# 2. 编辑 feeds.conf.default（或创建 feeds.conf 覆盖它）
#    在文件末尾追加以下一行：
#
src-git qmodem_failover https://github.com/YOUR_GITHUB/luci-app-qmodem-failover.git
#
# 3. 更新并安装 feeds
#    ./scripts/feeds update qmodem_failover
#    ./scripts/feeds install -a -p qmodem_failover
#
# 4. 在 menuconfig 中选择
#    make menuconfig
#    → LuCI → 3. Applications → luci-app-qmodem-failover
#    选 [*] 编译进固件，或 [M] 只生成 ipk
#
# 5. 编译
#    make -j$(nproc)
#    # 或只编译这个包：
#    make package/luci-app-qmodem-failover/compile -j$(nproc) V=s
#
# ─── 说明 ────────────────────────────────────────────────────
# PKGARCH:=all 表示本包架构无关（纯 Shell + Lua 脚本）
# 同一个 .ipk 文件适配所有 CPU 平台：
#   x86_64 / mipsel / mips64el / arm / aarch64 / armv7 等
# ────────────────────────────────────────────────────────────

